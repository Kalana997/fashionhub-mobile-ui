/** Placeholder `.env.example` mirrors optional runtime overrides referenced in middleware + seeds. */

# Optional cookie/session overrides for local experiments
FH_SESSION_COOKIE_NAME=fh_sid
FH_SESSION_HEADER_NAME=x-fh-session-id

# Used by seed utilities if expanded later — safe to omit for the default build
FH_TEST_USER_MARKER=_fh_test_seed

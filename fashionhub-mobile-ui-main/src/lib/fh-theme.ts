/** Primary CTA fill — unified across Explore, landing, checkout, detail. */
export const FH_ORANGE = "#FF7A00";

/** Frame top/bottom rules: same hue, slightly eased so it sits next to fills without yelling. */
export const FH_ORANGE_STROKE = "rgba(255, 122, 0, 0.82)";

/** Gray shell behind phone — warm-neutral to sit beside orange accents. */
export const FH_SHELL_GRAY = "#F6F5F4";

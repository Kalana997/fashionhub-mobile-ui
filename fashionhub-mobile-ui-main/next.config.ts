import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Product art lives in /public/images — optional remotePatterns if you swap back to CDN URLs. */
};

export default nextConfig;
